<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:ShadowRunHelperViewer"
             mc:Ignorable="d"
             x:Class="ShadowRunHelperViewer.UI.Controls.AdditionalValue">
    <ContentView.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <local:SharedStyles/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </ContentView.Resources>
    <ContentView.Content>
        <StackLayout Orientation="Horizontal" VerticalOptions="Start" Spacing="{StaticResource Spacing}">
            <BoxView Color="{StaticResource ForegroundColor}" WidthRequest="1"/>
            <StackLayout Spacing="0">
                <Label x:Name="NameDisplay" Style="{StaticResource STDText}" MaxLines="1" LineBreakMode="NoWrap"/>
                <Label x:Name="ValueDisplay" Style="{StaticResource STDText}"/>
            </StackLayout>
        </StackLayout>
    </ContentView.Content>
</ContentView>