<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:resx="clr-namespace:ShadowRunHelperViewer.Strings"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controlsother="clr-namespace:ShadowRunHelperViewer.UI.ControlsOther"
             mc:Ignorable="d"
             x:Class="ShadowRunHelperViewer.UI.ControlsOther.SettingsView">
    <ContentView.Content>
        <TableView>
            <TableView.Resources>
                <controlsother:SpacingConverter x:Key="SpacingConverter"/>
            </TableView.Resources>
            <TableRoot Title="{resx:UiResources TitleSettings}">
                <TableSection Title="{resx:UiResources Optionen_Group_Char}">
                    <SwitchCell Text="{resx:UiResources Optionen_EditAfterAdd}" On="{x:Binding Settings.START_AFTER_EDIT}"/>
                    <SwitchCell Text="{resx:UiResources Optionen_MINIMIZED_HEADER}" On="{x:Binding Settings.MINIMIZED_HEADER}"/>
                    <SwitchCell Text="{resx:UiResources Optionen_DisplayRequest}" On="{x:Binding Settings.DISPLAY_REQUEST}"/>
                </TableSection>
                <TableSection Title="{resx:UiResources Optionen_Group_IO}">
                    <SwitchCell Text="{resx:UiResources Optionen_LoadOnStart}" On="{x:Binding Settings.LOAD_CHAR_ON_START}"/>
                    <SwitchCell Text="{resx:UiResources Optionen_AutoSave}" On="{x:Binding Settings.AUTO_SAVE}"/>
                    <SwitchCell Text="{resx:UiResources Optionen_FORMAT_SAVEFILE}" On="{x:Binding Settings.FORMAT_SAVEFILE}"/>
                    <EntryCell Keyboard="Numeric" Label="{resx:UiResources Optionen_AutoSaveInterval}" Text="{x:Binding Settings.AUTO_SAVE_INTERVAL_MS}"/>
                </TableSection>
                <TableSection Title="{resx:UiResources Optionen_Group_File}">
                    <SwitchCell Text="{resx:UiResources Optionen_Ordnermode}" On="{x:Binding Settings.FOLDERMODE, Mode=TwoWay}"/>
                    <ViewCell>
                        <StackLayout Orientation="Horizontal" IsVisible="{x:Binding Settings.FOLDERMODE}">
                            <Label Text="{resx:UiResources Optionen_Ordnermode_Pfad}"/>
                            <Label Text="{x:Binding Settings.FOLDERMODE_PATH, Mode=OneWay}"/>
                        </StackLayout>
                    </ViewCell>
                    <SwitchCell Text="{resx:UiResources Optionen_BACKUP_VERSIONING}" On="{x:Binding Settings.BACKUP_VERSIONING}"/>
                </TableSection>
                <TableSection Title="{resx:UiResources Optionen_Group_Style}">
                    <ViewCell>
                        <StackLayout>
                            <Picker ItemsSource="{x:Binding Styles}" SelectedItem="{x:Binding Settings.CurrentStyleName}"/>
                            <Picker ItemsSource="{x:Binding Spacings}" SelectedItem="{x:Binding Settings.CurrentSpacingStrategy, Converter={StaticResource SpacingConverter} }"/>
                        </StackLayout>
                    </ViewCell>
                </TableSection>
            </TableRoot>
        </TableView>
    </ContentView.Content>
</ContentView>