<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="ShadowRunHelperViewer.ControlCenter">
    <ListView x:Name="MainListView" HasUnevenRows="True">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <ViewCell.ContextActions>
                        <MenuItem Text="Add" Icon="Add" Clicked="Add_Clicked"></MenuItem>
                        <MenuItem Text="Delete" Icon="Points" Clicked="Delete_Clicked"></MenuItem>
                        <MenuItem Text="Edit" Clicked="Edit_Clicked"></MenuItem>
                    </ViewCell.ContextActions>
                    <Grid>
                        <Label TextColor="Navy" Text="{Binding }" FontSize="25"></Label>
                    </Grid>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentView>